int Solution::solve(string A) {
    int n=A.size();
    int stack=0;
    for(int i=0;i<n;i++){
        if(A[i]=='(') stack++;
        else{
            stack--;
            if(stack<0) return 0;
        }
    }
    return stack==0;
}
