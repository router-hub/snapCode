int Solution::maxp3(vector<int> &A) {
    int n=A.size();
    if(n==1) return A[0];
    if(n==2) return A[0]*A[1];
    int one=-1, two=-1;
    sort(A.begin(),A.end());
    
    return max(A[n-1]*A[n-2]*A[n-3],A[n-1]*A[0]*A[1]);
}
